{% extends 'base.html.twig' %}

{% block title %}Anonce{% endblock %}

{% block content %}
    <div id="contain_all">
    <h1>Anonce</h1>
    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ anonce.id }}</td>
            </tr>
            <tr>
                <th>Titre</th>
                <td>{{ anonce.titre }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ anonce.description }}</td>
            </tr>
            <tr>
                <th>Categorie</th>
                <td>{{ anonce.categorie }}</td>
            </tr>
        </tbody>
    </table>

   <div id="contain_buttons">
       <a id="button_edit" href="{{ path('app_anonce_edit', {'id': anonce.id}) }}">Modifier</a>
       {{ include('anonce/_delete_form.html.twig') }}
    </div>
    </div>
{% endblock %}
