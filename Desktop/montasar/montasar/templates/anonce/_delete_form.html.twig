<form method="post" action="{{ path('app_anonce_delete', {'id': anonce.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ anonce.id) }}">
    <button class="btn">Delete</button>
</form>
