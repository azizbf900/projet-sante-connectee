<div class="card p-4 shadow-sm">
    {{ form_start(form) }}
        <div class="mb-3">
            {{ form_widget(form) }}
        </div>
        <button type="submit" class="btn btn-primary w-100">
            <i class="fas fa-check"></i> {{ button_label|default('Enregistrer') }}
        </button>
    {{ form_end(form) }}
</div>
